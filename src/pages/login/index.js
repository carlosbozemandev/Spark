import Login from "@/components/Layout/Auth/Login";
import React from "react";

const Auth = () => {
  return (
    <React.Fragment>
      <Login />
    </React.Fragment>
  );
};

export default Auth;
